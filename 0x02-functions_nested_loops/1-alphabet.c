#include <main.h>
/**
 * print_alphabet - call the alphabet
 *
 * Descrtiption: prints the alphabet, in lowercase, followed by a new line
 *
 * Return: 0 Always
 */
void print_alphabet(void)
{
	int a;

	for (a = 'a'; a <= 'z'; a++)
	{
		_putchar(n);
	}
	_putchar('\n');
}
